image:
  repository: ghcr.io/OWNER/genz-web
  tag: latest
  pullPolicy: IfNotPresent

replicaCount: 2

service:
  type: ClusterIP
  port: 3000

env:
  NEXT_PUBLIC_API_BASE_URL: "http://genz-api:8000"

resources: {}
